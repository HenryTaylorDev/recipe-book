import type { NextConfig } from "next";

module.exports = {
  images: {
    domains: ["ichef.bbci.co.uk", "images.immediate.co.uk"],
  },
};

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
